# 🚀 VULTRA DEPLOYMENT COMMANDS
# Server IP: 65.20.71.151

## STEP 1: SSH into Vultr Server
Open Terminal on your Mac and run:
```bash
ssh root@65.20.71.151
```
(Enter your root password when prompted)

---

## STEP 2: Clean Everything
Run these commands ONE BY ONE:
```bash
cd /
docker stop $(docker ps -aq) 2>/dev/null || true
docker rm $(docker ps -aq) 2>/dev/null || true
docker rmi -f $(docker images -q) 2>/dev/null || true
systemctl stop docker
rm -rf /var/lib/docker/*
systemctl start docker
sleep 10
rm -rf /vaidya-vihar-diagnostic 2>/dev/null || true
```

---

## STEP 3: Clone Fresh
```bash
git clone https://github.com/sujitsinghds54970/vaidya-vihar-diagnostic.git /vaidya-vihar-diagnostic
cd /vaidya-vihar-diagnostic
```

---

## STEP 4: Build and Start
```bash
docker compose up -d --build
```

**Wait 10-15 minutes for build to complete**

---

## STEP 5: Check Status
```bash
docker compose ps
```

---

## STEP 6: Access Application

| Service | URL |
|---------|-----|
| Frontend | http://65.20.71.151 |
| API | http://65.20.71.151:8000 |
| API Docs | http://65.20.71.151:8000/docs |

---

## Login Credentials
- Username: **admin**
- Password: **admin123**

---

## Useful Commands
```bash
# View logs
docker compose logs -f

# Restart
docker compose restart

# Stop
docker compose down

# Update (after code changes)
git pull && docker compose down && docker compose up -d --build
```

---

## Troubleshooting
```bash
# Check logs
docker compose logs -f backend
docker compose logs -f frontend

# Check container status
docker compose ps

# SSH into backend
docker compose exec backend sh

# Check database
docker compose exec postgres psql -U vaidya_user -d vaidya_vihar_db
```

