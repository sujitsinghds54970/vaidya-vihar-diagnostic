# 🚀 VULTRA DEPLOYMENT GUIDE FOR VAIDYAVIHAR DIAGNOSTIC ERP

## YOUR SERVER DETAILS
- **Server IP:** 65.20.71.151
- **Server Location:** Vultr (Cloud Server)
- **Access Method:** SSH (Terminal)

---

## STEP-BY-STEP DEPLOYMENT INSTRUCTIONS

### 📋 STEP 1: Open Terminal on Your Mac
1. Press `Cmd + Space` (Spotlight Search)
2. Type `Terminal`
3. Press `Enter`
4. You should see a black window with text

---

### 📋 STEP 2: Connect to Your Vultr Server

Copy and paste this command into Terminal:

```
ssh root@65.20.71.151
```

Press **Enter**

When it says `Are you sure you want to continue connecting?`, type `yes` and press **Enter**

When it asks for password, enter your **Vultr root password** and press **Enter**
(Note: Password won't show as you type - that's normal!)

---

### 📋 STEP 3: Wait for Connection
If successful, you should see something like:
```
Welcome to Ubuntu 22.04 LTS
root@vaidyavihar-erp:~#
```

The text `root@vaidyavihar-erp:~#` means you're now on your Vultr server!

---

### 📋 STEP 4: Run Deployment Script

Copy ALL these commands together and paste them:

```
cd ~ && rm -rf vaidya-vihar-diagnostic && git clone https://github.com/sujitsinghds54970/vaidya-vihar-diagnostic.git && cd vaidya-vihar-diagnostic && chmod +x QUICK_DEPLOY.sh && ./QUICK_DEPLOY.sh
```

Press **Enter**

This will take **5-10 minutes**. You'll see progress messages.

---

### 📋 STEP 5: Wait for Installation to Complete

When finished, you should see:
```
🎉 DEPLOYMENT COMPLETE!
```

---

### 📋 STEP 6: Access Your Application

Open a NEW tab in your web browser (Chrome/Safari/Firefox) and go to:

```
http://65.20.71.151
```

---

### 📋 STEP 7: Login

You should see the login page. Enter:

- **Username:** admin
- **Password:** admin123

---

## 🆘 TROUBLESHOOTING

### "Command not found" or "Permission denied"
- Make sure you copied the entire command
- Check your internet connection

### "Connection refused"
- Wait a few minutes and try again
- The server might still be installing

### Forgot Vultr password
- Go to https://my.vultr.com
- Click on your server
- Click "View Console" to reset password

---

## 📱 USEFUL COMMANDS (After Deployment)

View logs:
```
docker compose logs -f
```

Restart services:
```
docker compose restart
```

Stop everything:
```
docker compose down
```

---

## 🌐 Service URLs

| Service | URL |
|---------|-----|
| **Frontend** | http://65.20.71.151 |
| **API Docs** | http://65.20.71.151:8000/docs |

---

**Good luck! 🎉**

